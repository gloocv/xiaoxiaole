
#include"ControlNode.h"

USING_NS_CC;

void ControlNode::update(float dt)
{
	if (m_mode == CONTROLMODE::STOP)
		return;
	else if (m_mode == CONTROLMODE::TOUCHMOVE)
	{
		
	}
	else if (m_mode == REMOVE)
	{

	}
	else if (m_mode == MOVEDOWN)
	{

	}
}